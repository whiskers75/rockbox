before_install:
   - mkdir build
   - cd build
   - sudo apt-get install flex texinfo build-essential -qq
   - export BUILDFOR=62
   - sudo ../tools/rockboxdevbinutils.sh 1> /dev/null 2>/dev/null
   - sudo ../tools/rockboxdevgcc.sh 1> /dev/null 2>/dev/null
   - echo "Running configure"
   - ../tools/configurequiet > /dev/null

install: echo "DEPENDENCIES INSTALLED YAY"

script: make

language: c
